# Gap 4: 系统应用回读 — cognitive.md 改动规范

> **日期**: 2026-05-17
> **优先级**: P0（闭环断裂，不补则存储的数据都是死的）
> **改动目标**: cognitive.md（.claude/commands/cognitive.md）
> **原则**: 只写改动内容，不修改原文

---

## 改动总览

| # | 改动 | 插入位置 | 影响范围 |
|---|------|---------|---------|
| 1 | 启动流程新增 Step 2.5 | 启动流程 Step 2 与 Step 3 之间 | 启动流程 |
| 2 | 情绪容器注入历史洞察 | 情绪容器模式 Step 3 内 | 情绪容器 |
| 3 | ACTION_BLOCK 调用决策框架 | ACTION_BLOCK 策略路由详情内 | 策略路由 |
| 4 | 对话中持续检索 | 对话中的持续检测部分末尾 | 持续检测 |

---

## 改动 1：启动流程新增 Step 2.5 — 语义检索相关历史洞察

### 插入位置

在 cognitive.md 启动流程中，Step 2（`读取 memory/short-term/ 最近 3 个文件`）之后、Step 3（`读取 memory/long-term/`）之前，插入新步骤。

### 插入内容

```markdown
2.5. 语义检索相关历史洞察
  - 读取 cognitive-db/why-reasons/ — 检查是否有与当前输入相关的历史原因
  - 读取 cognitive-db/how-methods/ — 检查是否有适用的历史方法
  - 读取 cognitive-db/events/ — 检查是否有相似的历史事件
  - 读取 cognitive-db/cognitive-models/ — 检查是否有匹配的认知模型
  - 读取 cognitive-db/decision-frameworks/ — 检查是否有适用的决策框架
```

### 匹配方式

检索基于以下信号从用户输入中提取关键词，与各库条目的结构化字段进行匹配：

| 用户输入信号 | 匹配目标 | 匹配字段 |
|-------------|---------|---------|
| 标签关键词（用户描述中的核心概念） | 所有库 | `tags` 字段 |
| 涉及的信念（用户表达中隐含的信念） | why-reasons | `涉及的信念` 字段 |
| 触发场景关键词（"当我...时"、"每次..."） | why-reasons, cognitive-models | `触发场景` / `触发条件` 字段 |
| 情绪词（焦虑/崩溃/受不了/烦/累/难过...） | why-reasons | `reason_type: emotion` |
| 问题模式（"我总是..."、"为什么我..."） | cognitive-models | `触发条件` 字段 |
| 行为描述（"我就是做不到"、"又拖延了"） | how-methods | `适用场景` 字段 |
| 决策困境（"不知道该..."、"纠结..."） | decision-frameworks | `适用场景` 字段 |

### 检索结果注入策略

| 匹配强度 | 判定条件 | 注入方式 |
|---------|---------|---------|
| 强匹配 | 标签命中 + 场景关键词命中 | 对话中主动提及："之前你遇到类似情况时，[原因/方法]——这次感觉怎么样？" |
| 弱匹配 | 仅标签命中，或仅场景关键词命中 | 准备为上下文背景，不主动提及，但在追问时参考 |

**约束**：
- 同一会话中，同一条目只注入一次，不重复提及
- 如果用户输入为空（首次引导模式），跳过本步骤
- 如果 cognitive-db 各库为空（新用户），跳过本步骤

---

## 改动 2：情绪场景自动提醒

### 插入位置

在 cognitive.md 的"情绪容器模式"中，Step 3（`温和追问`）部分内部追加历史洞察注入逻辑。

### 原始 Step 3 内容（不变）

```markdown
3. **温和追问**（情绪缓和后，intensity ≤7 时）
   - "你觉得是什么让你这么难受？"
   - 不追问"为什么你会有这种感觉"（太分析性）
   - 只问"是什么让你..."（描述性，压力更小）
```

### 追加内容

在 Step 3 的追问选项列表末尾追加：

```markdown
   - 如果 why-reasons/ 中有 reason_type: emotion 的条目匹配当前情绪信号：
     "之前你也有过类似的感受，当时是因为 [原因]——这次是一样的吗？"
     - 不强推分析，只提供一个参考锚点
     - 如果用户否定关联（"这次不一样"），立即放弃此锚点，回到纯共情
     - 如果用户确认关联，可以自然过渡："那当时是什么帮到你的？"（连接 how-methods）
```

### 匹配判定

当检测到 EMOTION_RELEASE 状态时，同时执行轻量检索：
1. 从用户输入中提取情绪关键词
2. 在 why-reasons/ 中筛选 reason_type: emotion 的条目
3. 用情绪关键词匹配条目的 tags 和 触发场景 字段
4. 命中则视为匹配

### 约束

- 仅在 intensity ≤7 时注入（intensity ≥8 时只共情，不做任何历史引入）
- 一次情绪容器会话中最多注入一个历史锚点
- 用户否定后不再就同一条目二次引入

---

## 改动 3：行动场景自动调用策略

### 插入位置

在 cognitive.md 的"ACTION_BLOCK 策略路由详情"中，在"首次进入 ACTION_BLOCK"子段之前，插入框架匹配检查。

### 插入内容

在 ACTION_BLOCK 策略路由详情开头（"首次进入 ACTION_BLOCK"之前）追加：

```markdown
**决策框架匹配检查**（进入 ACTION_BLOCK 时首先执行）：

在 dispatch Action Agent 之前，检查 decision-frameworks/ 中是否有匹配当前场景的框架：
1. 从用户输入和当前状态中提取场景关键词
2. 与 decision-frameworks/ 各条目的 `适用场景` 字段匹配
3. 如果有匹配的框架 → 传递给 Action Agent：

```
操作: generate-micro-action

模式报告:
[Pattern Detector 输出的模式报告]

用户已构建的决策框架:
框架名称: [框架名称]
决策步骤: [框架的决策步骤]
关联方法: [框架的关联方法]

指令: 基于该决策框架的步骤生成微行动，而非从零设计。
```

4. 如果没有匹配的框架 → 按原有逻辑（从 Pattern Detector 报告生成）
```

### 约束

- 框架匹配为辅助增强，不替代 Pattern Detector 报告
- Action Agent 仍需结合用户当前具体表述调整方案，不机械套用框架步骤
- 如果用户对框架生成的行动方案表示不适，回退到从零设计

---

## 改动 4：对话中持续检索

### 插入位置

在 cognitive.md 的"对话中的持续检测"部分末尾追加。

### 原始内容（保留）

```markdown
## 对话中的持续检测

状态不是一次判断就固定的。在对话过程中持续评估：

- 用户从发泄转向提问 → EMOTION_RELEASE → PROBLEM_EXPLORATION
- 用户从提问转向自我觉察 → PROBLEM_EXPLORATION → COGNITIVE_REFLECTION
- 用户从反思转向行动受阻 → COGNITIVE_REFLECTION → ACTION_BLOCK
- Pattern Engine 深度确认检测到新模式 → 任何状态 → ACTION_BLOCK
- 用户重新出现强情绪 → 任何状态 → EMOTION_RELEASE（优先级最高）
- 行动反馈成功 → ACTION_BLOCK → COGNITIVE_REFLECTION 或 ENTRY_RECORD
- 行动反馈失败 → 保持在 ACTION_BLOCK，进入阻力分析

每次状态变化时，调整对话策略，不需要明确告诉用户"你的状态变了"。
```

### 追加内容

在上述内容末尾追加：

```markdown

### 持续语义检索

每次用户输入后（不仅是启动时），执行轻量检索：

1. 从当前输入中提取关键词（标签 + 情绪词 + 场景词 + 信念词）
2. 在 cognitive-db 各库中快速匹配
3. 如果发现强匹配且本次会话中尚未提及：
   - 自然引入历史洞察："之前你提到过 [相关内容]..."
   - 或在追问时参考相关条目的信息
4. 如果仅弱匹配 → 作为上下文背景准备，不主动提及

**去重约束**：
- 同一条目在同一会话中只注入一次
- 维护一个已注入条目列表（条目文件名即可），每次检索前检查
- 如果用户对某条历史洞察表示"不一样"或"这次不同"，该条目标记为已拒绝，不再提及
```

### 检索时机细化

| 时机 | 检索范围 | 目的 |
|------|---------|------|
| 启动时（Step 2.5） | 全库 | 建立初始上下文 |
| 每次用户输入后 | 全库（轻量扫描） | 捕获新话题的相关历史 |
| 状态转换时 | 对应库（见下） | 为新策略提供支撑 |
| 追问时 | 弱匹配结果的库 | 参考已准备但未注入的背景 |

状态转换时的定向检索：

| 转入状态 | 检索库 | 目的 |
|---------|-------|------|
| EMOTION_RELEASE | why-reasons (emotion) | 提供情绪历史锚点 |
| PROBLEM_EXPLORATION | why-reasons, cognitive-models | 连接历史原因和认知模型 |
| COGNITIVE_REFLECTION | cognitive-models, how-methods | 连接已有模型和方法 |
| ACTION_BLOCK | decision-frameworks, how-methods | 调用框架和方法生成行动 |
| FAILURE_REVIEW | events, why-reasons | 连接历史事件和原因 |

---

## 不改动的部分

以下部分本次不改动，确认不影响：

- 状态检测（6 种状态、检测维度、检测算法）— 不变
- 状态转移约束 — 不变
- 追问引导规则 — 不变（改动 2 仅在情绪容器内追加，不修改追问规则本身）
- 存储逻辑 — 不变（回读不影响写入路径）
- 首次使用引导 — 不变
- 参数 — 不变

---

## 实施检查清单

- [ ] 在 cognitive.md 启动流程 Step 2 后插入 Step 2.5（含匹配方式表和注入策略表）
- [ ] 在情绪容器 Step 3 末尾追加历史洞察注入逻辑
- [ ] 在 ACTION_BLOCK 策略路由详情开头插入框架匹配检查
- [ ] 在"对话中的持续检测"末尾追加持续语义检索子段
- [ ] 验证 4 处插入后 cognitive.md 的 Markdown 结构完整（标题层级、列表缩进无错位）
- [ ] 验证新用户（cognitive-db 为空）场景下所有新增逻辑均安全跳过
- [ ] 验证同一条目去重机制在 4 个改动点之间一致
